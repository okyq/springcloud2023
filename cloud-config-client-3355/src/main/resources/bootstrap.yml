spring:
  cloud:
    #Config客户端配置
    config:
      label: main #分支名称
      name: config #配置文件名称
      profile: dev #读取后缀名称   上述3个综合：master分支上config-dev.yml的配置文件被读取http://config-3344.com:3344/master/config-dev.yml
      uri: http://localhost:3344 #配置中心地址k
management:
  endpoints:
    web:
      exposure:
        include: "*"

#把下面的信息都放到git的配置文件中,bootstrap会在初始化时加载配置文件
#spring:
#  application:
#    name: config-client
#server:
#  port: 3355
##服务注册到eureka地址
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:7001/eureka